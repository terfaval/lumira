# Legacy Transition Archive

`docs/archive/legacy-transition/` is historical reference material.

This folder contains:

- compatibility contracts
- projection contracts
- rollout/parity/read-switch plans
- route/API ownership migration plans
- transition audits and bridge strategies

Do not use these files as primary clean-room build context unless explicitly requested by the ticket owner.

Legacy compatibility is not canonical truth.
Route/API migration plans are not clean-room architecture.