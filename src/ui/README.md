# UI Layer

Presentation-only components and styling live here.
UI must render bounded reflective structures and avoid owning cognition/orchestration.
Reflective Space workspace UI may compose calm read-only traces and explicit user actions, but must not add hidden orchestration.
