# Supabase Repositories

## Legacy Backend V1 Quarantine

These repositories remain active only because protected routes and pages still depend on the current backend substrate.

They are not clean-room Backend V2 authority and must not define the starting ontology for future Backend V2 implementation.
